# TT-Component

```{note}
This is the starter page from the Tenstorrent component docs template. Replace it
with your component's documentation, authored in MyST Markdown. See the template
README for build and publish details.
```

Describe what your component does, how to install and configure it, and how to
verify it is working. Add more pages and a `{toctree}` as the documentation
grows.

<!--
If your component ships a helm chart with a helm-docs-generated README, set
`chart_readme` (DOCS_CHART_README) in conf.py and add a Configuration page that
renders it verbatim:

```{include} _generated/chart-values.md
:heading-offset: 1
```
-->
